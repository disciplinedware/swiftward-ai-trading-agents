# Dependencies
golang/vendor/
ruby/.bundle/
ruby/vendor/
python/.venv/
python/__pycache__/
python/**/__pycache__/
python/*.egg-info/
typescript/node_modules/
typescript/dist/
golang/internal/dashboard/dist/*
!golang/internal/dashboard/dist/placeholder.html

# Build artifacts
golang/bin/
golang/server
rust/target/
*.exe
*.dll
*.so
*.dylib

# Environment
.env
.env.*
.env.local
.env.*.local

# IDE
.idea/
# .vscode/
.claude/settings.local.json
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker
docker/.env

# Local data (Docker volume bind mounts)
data/

# Logs
*.log
logs/

# Test artifacts
coverage/
.coverage
htmlcov/

# ralphex progress logs
.ralphex/progress/
coverage.out

# Solidity / Foundry — compiled artifacts and cache (regenerated by forge build)
# broadcast/ is kept — it's the deployment receipt (contract address, tx hash, block)
# lib/ contains git submodules — clone with --recurse-submodules or run: git submodule update --init
solidity/out/
solidity/cache/
golang/agent-intel

# Presentation build workspace (PPTX + node_modules + temp HTML)
presentation/workspace/

# Local video files - too large for Cloudflare Pages (25 MiB limit)
videos/
landing/*.mp4

# Submission form working notes (staging for lablab.ai / Surge / YouTube forms)
docs/hackathon/surge-submission.md
docs/hackathon/youtube-submission.md
docs/hackathon/lablab-submission.md
